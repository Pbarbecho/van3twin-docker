# Contexto del proyecto SUMO_GEO (GEO SUMO WEB)

> Contexto de trabajo para la migración/integración con VaN3Twin.
> Complementa `GUIA_INTEGRACION_SUMO_GEO.md` y `CONTEXTO_VAN3TWIN.md`.
> Código local: `/Users/Pablo_1/Dropbox/Mac/Documents/SUMO_GEO` · Repo: https://github.com/Pbarbecho/SUMO_GEO

## Qué es

Aplicación web para **ejecutar y visualizar simulaciones SUMO en el navegador**: edificios 3D, vehículos 3D procedurales (LOD automático), semáforos sincronizados con SUMO, estimación de tráfico LOS (A–F) por calle, inspector de clic derecho y panel de históricos. Stack: **FastAPI (backend) + MapLibre GL JS + deck.gl (frontend, sin build step) + nginx**. Prototipo de investigación de la Universidad de Cuenca.

```
SUMO ──TraCI (suscripciones)──► backend FastAPI ──REST────► red + edificios + semáforos (GeoJSON, cacheado)
                                              └──WebSocket► vehículos + LOS + semáforos + stats por paso
                                                            └► nginx :8081 ► navegador (MapLibre + deck.gl)
```

## Estructura

| Ruta | Rol |
|---|---|
| `backend/app/config.py` | `Settings` (pydantic-settings, prefijo `APP_`, `.env`). |
| `backend/app/main.py` | FastAPI: REST + WS `/ws/live`. Un `SumoBridge` **por conexión WS**. |
| `backend/app/sumo_bridge.py` | Wrapper TraCI/libsumo: start/step/vehicles/stats/inspector/tls. |
| `backend/app/geo.py` | `NetworkGeo` (net→GeoJSON, xy→lonlat), `cfg_paths()`, edificios, semáforos. |
| `backend/app/traffic.py` | Densidad por edge → LOS HCM (A–F) + color. Solo edges activos (O(vehículos)). |
| `backend/Dockerfile` | python:3.11-slim + libs GL/X11 + `eclipse-sumo` (modo managed). |
| `backend/requirements.txt` | fastapi 0.115.6, uvicorn 0.34.0, websockets 14.1, **eclipse-sumo/traci/sumolib==1.27.1**, pyproj. |
| `frontend/app.js` (~980 líneas) | Toda la lógica del visor: capas deck.gl, LOS, semáforos, presets de luz, inspector, históricos. |
| `frontend/nginx.conf` | Proxy same-origin: `/api/` y `/ws/` → `backend:8000`; resto estático. |
| `docker-compose.yml` | Servicios `backend` (:8000) y `frontend` (nginx :8081). Monta `./sumo` y `./mapa` (RO). |
| `sumo/`, `mapa/` | Escenarios: `metro_{low,mid,high}` (toda Cuenca, 24 h), `cuenca*` (OSM con edificios), `demo`. |
| `scripts/` | `build_city.sh` (OSM→net+poly), `gen_traffic.py`, `enrich_heights.py`. |

## Configuración (`Settings`, prefijo `APP_`)

- `sumo_mode`: `managed` (backend lanza SUMO; default) | `remote` (conecta a TraCI ya corriendo).
- `sumo_binary`, `sumo_config` (`/sumo/demo.sumocfg`), `sumo_host` (`sumo`), `sumo_port` (**8813**), `use_libsumo`, `step_length` (1.0).
- `sumo_config_template` (`/mapa/metro_{level}.sumocfg`) + `sim_begin`: selector Bajo/Medio/Alto vía `?level=` del WS.
- `net_file`/`poly_file`: overrides opcionales; **si falta cualquiera de los dos, `lifespan` parsea el `.sumocfg`** (`cfg_paths`) — relevante para el truco `APP_POLY_FILE=/no_poly.xml` de la guía.
- `origin_lon/lat` (ancla ENU para redes sin proyección), `view_lon/lat` (centro inicial), `max_fps` (10), `cors_origins`.

## Flujo en runtime

**Arranque (lifespan)**: carga la red una vez (`NetworkGeo`), cachea GeoJSON de edges, edificios y semáforos, y `meta` (center/bounds/origin/step_length/mode).

**REST**: `/api/health`, `/api/meta`, `/api/network`, `/api/buildings`, `/api/trafficlights`.

**WS `/ws/live`**: acepta → crea `SumoBridge` → `bridge.start(config, begin)` → envía `{"type":"meta",...}` → bucle: `step()` → `vehicles()` → frame `{"type":"frame","t","vehicles","edges","tls","stats"}` → `sleep(1/fps)`. Fin de simulación (`getMinExpectedNumber()<=0`) → `{"type":"end"}`. Comandos del cliente: `pause`, `play`, `speed{fps}`, `inspect{id}` (responde `{"type":"inspect",...}`). Error en start → `{"type":"error"}` + close.

**`SumoBridge` claves**:
- Suscripciones TraCI `_SUB_VARS` (POSITION, ANGLE, SPEED, TYPE, ROAD_ID, CO2, WAITING): flota completa en 1 round-trip; fallback a polling.
- `start()` rama remote: `client.init(host, port)` — **hoy sin `setOrder`** (módulo `traci` global, sin label).
- `step()`: **hoy `simulationStep()` a secas** (un paso por frame).
- Cachea dimensiones por vType; agrega CO₂/espera/travel-time para el panel histórico.
- `close()` cierra la conexión TraCI al desconectarse el WS.

**Coordenadas** (`geo.py`): red con proyección real (OSM) → `net.convertXY2LonLat`; red sintética → aproximación ENU anclada en `origin_lon/lat`.

## Escenario activo en `docker-compose.yml`

Modo `managed`, escenario metro (`/mapa/metro_mid.sumocfg`, template `metro_{level}`), vista en el centro de Cuenca, `APP_SIM_BEGIN=28800` (08:00). Alternativa comentada: `cuenca*` OSM con edificios. Puertos: backend `8000:8000`, frontend `8081:80` (el comentario de cabecera dice 8080 — desactualizado, el puerto real es **8081**).

## Estado respecto a la migración VaN3Twin

**Pasos 2–4 APLICADOS (2026-08-25):**

1. ✅ `config.py`: `sumo_order: int = 0` (línea 39).
2. ✅ `sumo_bridge.py` `start()` remote: `client.setOrder(settings.sumo_order)` si `sumo_order>0`.
3. ✅ `sumo_bridge.py` `step()`: en remote+order, objetivo absoluto `simulationStep(getTime() + step_length)`.
4. ✅ Nuevos: `backend/requirements-van3twin.txt` (**traci/sumolib==1.12.0**, sin `eclipse-sumo`), `backend/Dockerfile.van3twin`, `docker-compose.van3twin.yml` (red `van3twin_default` y volumen `van3twin_ns3-workspace` externos; `APP_SUMO_HOST=van3twin`, `APP_SUMO_PORT=3400` — el EVA fija SumoPort=3400, no el default 1338 —, `APP_SUMO_ORDER=2`, net del EVA desde el volumen RO).

**INTEGRACIÓN VALIDADA Y FUNCIONANDO (25-ago-2026):** visor 3D sobre Turín en lockstep con VaN3Twin. Procedimiento operativo completo en `MANUAL_SIMULACION_VAN3TWIN_SUMO_GEO.md`.

Cambios finales adicionales en este repo (post-guía, todos verificados):

1. `config.py`: `sumo_order` y `sumo_port_scan` (nuevo: nº de puertos extra a probar).
2. `sumo_bridge.py` `start()` remote: bucle de conexión sobre `sumo_port..sumo_port+scan` con `numRetries=1` + `setOrder(order)`. Motivo: el `GetFreePort()` de ns-3 corre el puerto (3400→3401…) cuando el anterior queda en TIME_WAIT entre corridas.
3. `sumo_bridge.py` `step()`: objetivo absoluto `simulationStep(getTime()+step_length)` en remote+order.
4. `geo.py`: no usar `net.hasGeoProj()` — **no existe en sumolib ≤1.12** y el `except` lo dejaba en False, dibujando la red de Turín anclada en Cuenca (bug visto en vivo). Ahora se prueba `convertXY2LonLat(0,0)` directamente.
5. Nuevos: `backend/requirements-van3twin.txt`, `backend/Dockerfile.van3twin`.
6. **Interpolación de movimiento en el frontend (25-ago, noche):** `app.js` interpola posición/rumbo entre frames vía `requestAnimationFrame` (estado: `frameVehicles`/`interpPrev`/`interpT0`/`interpPeriod`; helpers `angleLerp`, `metersBetween`; tope `INTERP_MAX_VEH=1500`, anti-teleport `INTERP_MAX_JUMP=80 m`). Refinamientos posteriores: los ticks refrescan **solo la capa `vehicles`** (`makeVehiclesLayer` + `refreshVehiclesLayer`, capas restantes reutilizadas por identidad), ritmo adaptativo por tamaño de flota (`interpMinInterval`), y **bugfix clave**: el handler de frame NO asigna `vehicles = msg.vehicles` (pintar la posición nueva causaba salto+retroceso); solo fija `frameVehicles` como objetivo. `nginx.conf` sirve estáticos con `Cache-Control: no-store` (evita depurar contra app.js cacheado). `APP_STEP_LENGTH` default del modo van3twin bajó a **0.5**. Sin cambios en el backend.
7. **Replay offline de mensajes V2X (25-ago, noche):** nuevo `backend/app/replay.py` — parsea los `.pcap` de VaN3Twin (DLT 105, LLC/SNAP 0x8947, GN Basic(4)+Common(8)+SO PV(24)+res(4), BTP-B en gn[40], payload en gn[44]; puertos 2001 CAM/2002 DENM/2009 CPM), reconstruye movilidad desde el SO PV (lat/lon/speed/heading del emisor), eventos TX/RX, PDR por pares, y decodifica contenido ASN.1 **bajo demanda** con asn1tools + los `.asn` del árbol (CAM: `asn1-v2/EN302637-2v141-CAM.asn`+CDD; **CPM: `full-v1-v2/CPM-all.asn`, top `CollectivePerceptionMessage` — la variante TR103562+ISO19091 tarda minutos en compilar**). El pool de nodos ns-3 se REUTILIZA entre vehículos SUMO → mapeo nodo→stationID por segmentos temporales decodificando todos los CAM propios. WS: `?replay=1` en `/ws/live` (reproductor con seek; `_ws_replay` en main.py), REST `/api/replay/info`. Clave del tipo en `msg_detail` es `mtype` (un `"type"` en el `**det` pisaba el `"type":"msg_detail"` — bug histórico). Frontend: botón Replay, timeline, capas `v2x-pulse`/`v2x-arc` animadas en el tick de interpolación, clic en pulso → popup con contenido. Compose: `./results:/replay:ro` + `APP_REPLAY_PATTERN`. Extras (25-ago, madrugada): cmd WS `step` (un frame y re-pausa; flag `single` en `_ws_replay`), selector de vehículo emisor (`selStation`, `#veh-filter`, filtra por `e.st` en pulsos y arcos), y modo congelado — con `replayMode && paused` los mensajes no se desvanecen (prog fijo 0.35, sin prune) y los vehículos saltan al frame sin interpolar. Todo validado end-to-end en sandbox contra los pcaps reales del EVA (8.861 TX / 47.852 RX, carga 0.5 s; step: 1 frame exacto por comando). UI: los controles de replay viven en un **panel propio `#replay-panel` ("Replay V2X")** en la columna izquierda bajo el panel SUMO·GEO (`#left-col` flex; ambos con auto-colapso). Capturas PNG del visor para documentos: pipeline reproducible en `CONTEXTO_CAPTURAS_WEB.md` + `tools/captura_web.py` (stack completo en sandbox + Playwright headless; imágenes en `img/`). Documento didáctico aparte: `Practica_Replay_V2X.tex/pdf` (replay web vs Wireshark, con marco teórico ETSI CAM/DENM/CPM/ASN.1-UPER + tabla de unidades, tabla de contraste y ejercicios); el manual solo referencia. Refinamientos (26-ago): arcos clicables (decode tolerante ±20 ms con hint de tipo — el t del arco es de RECEPCIÓN, hasta ~16 ms tras el TX por cola/backoff; ventana de receptores 30 ms), popup con clase `interactive` (el `#popup` base lleva `pointer-events:none` — por eso no se podía hacer scroll; la clase lo habilita + botón ✕), y cmd `step_back` (t = t−2·step y un frame) con botones icon-only ⏮/⏭. Refinamientos (26-ago, 2ª tanda): `decode()` devuelve `layers` (802.11/GeoNetworking-SO PV/BTP; `_payloads` guarda `(payload, meta)` — ojo al desempaquetar) y el popup los muestra como `<details>` plegables estilo Wireshark; pin 📌 en los paneles (`.pin` inyectado por JS en el h1, estado en localStorage `pin-<id>`, bloquea el auto-colapso). **Panel PHY 802.11p** (26-ago, 3ª tanda): `replay.py.phy_stats()` (cacheada) mide latencia TX→RX (emparejando rx→tx por estación/tipo/±30 ms), rango de cobertura (distancia emisor-receptor vía trayectorias `_pos_at`), PER global (esperadas = coexistencia por vida de trayectoria), PDR clampado a 1.0 (bordes de reutilización del pool duplican alguna RX), tasas y utilización de canal por airtime (~40 µs + bytes/12 Mbps); config PHY del EVA como constantes; SIN potencia RX (pcap sin radiotap — documentado). Expuesta en `/api/replay/info` clave `phy`; frontend `#phy-panel` + `loadPhyStats()` (fetch al entrar en replay). **Recarga automática del índice (26-ago):** `_get_replay()` compara una huella (path, mtime, size) de pcaps+signal*.csv del replay_dir y recarga si cambió — ya NO hace falta `restart backend` al copiar una corrida nueva a `~/results` (validado: 2ª consulta detectó el CSV nuevo sin reinicio). Valores de referencia de la corrida de prueba: lat p50 0,14 ms/máx 27 ms, cobertura p95 183 m/máx 311 m, PER 13,7 %, canal ~0,7 %. **RSSI + distancia en enlaces (26-ago, 4ª tanda):** `_load_signal()` ingiere `signal*.csv` opcional del replay_dir (formato `rx,tx,t_ms,rssi[,snr]`; cabeceras flexibles; heurística ms/s; asocia por (tx,rx) ±50 ms) → `e["rssi"]` en eventos RX, `receivers_info` (dist vía `_pos_at` + rssi) en `decode()`, bloque `rssi_dbm` y nota dinámica en `phy_stats()`. Frontend: TextLayer `v2x-arc-lbl` con "N m · X dBm" en el punto medio del arco (visible si frozen o ≤40 arcos), popup con receptores (dist, rssi), fila RSSI en panel PHY. Generación del CSV: parche al EVA con `addCARxCallbackExtended`/`addCPRxCallbackExtended` (SignalInfo: timestamp/rssi/snr/sinr/rsrp/size; ambos callbacks conviven — verificado en caBasicService.cc:381) — código en la práctica. **Corrección PHY (26-ago):** a 10 MHz, 12 Mbit/s = **16-QAM 1/2** (no QPSK 1/2 — eso es 6 Mbit/s en ese BW); corregido en `phy_stats()` config y en la práctica, que ahora incluye teoría RSSI/log-distancia con cálculos y la tabla de sensibilidades IEEE por tasa. **Gotchas del parche C++ (vistos al compilar):** el campo del header ASN.1 es `stationId` (d minúscula, no `stationID` como en el decode Python); `logSignalCSV` debe definirse ANTES de `StartApplication`; la lambda del CPM necesita su firma propia (`Seq<CollectivePerceptionMessage>`, `StationID_t`) — no reutilizar la del CAM. Validado end-to-end con CSV sintético (28.595 muestras asociadas). Al seleccionar un vehX en el filtro, el vehículo se **resalta con anillo ámbar** en el mapa (capa `veh-selected` en `makeHighlightLayer()`, incluida en refresh completo y dinámico).
8. **Respaldo y docencia (28-ago):** repos en GitHub — `Pbarbecho/SUMO_GEO` (main), `Pbarbecho/VaN3TwinGEO` (fork ns-3 con los 8 parches en master), `Pbarbecho/van3twin-docker` (stack+docs, con README de réplica y RESPALDO.md). Compose parametrizado para réplica: ruta del visor vía `${SUMO_GEO_DIR:-../SUMO_GEO}` (Pablo la fija en `.env` local, no versionado → estudiantes clonan los repos lado a lado) y `VAN3TWIN_REPO` build-arg apuntando al fork (Dockerfile parametrizado; los rebuilds salen CON los parches). Nueva `Practica_Instalacion_Framework.tex/pdf` (9 págs): marco teórico con repaso git/docker, trabajo previo (build de 1-3 h antes de clase), instalación por SO (Linux/WSL2/macOS, con la advertencia case-insensitive), y verificación por niveles 0-5 (contenedor → SUMO solo → ns-3 solo → visor solo managed → integración → replay).
9. **Compose unificado (25-ago, tarde):** los servicios `backend`/`frontend` del modo van3twin viven ahora en `~/van3twin-docker/docker-compose.yml` bajo el **profile `visor`** (`docker compose --profile visor up -d`; build context absoluto al repo SUMO_GEO; volumen `ns3-workspace` interno, sin red/volumen externos). `SUMO_GEO/docker-compose.van3twin.yml` fue **eliminado** (25-ago). Env: `APP_SUMO_PORT=3400`, `APP_SUMO_PORT_SCAN=10`, `APP_SUMO_ORDER=2`.

10. **Marca institucional UCuenca (29-ago):** frontend re-tematizado con la paleta de la Universidad de Cuenca (los mismos colores de los documentos LaTeX del curso): azul `#002856` y rojo `#A51008`. En `style.css` los tokens viven en `:root` (`--ucu-azul`, `--ucu-rojo`, `--panel:#002856e6`, `--bg:#001229`, `--btn:#0d3a72`, `--btn-hover:#14508f`, `--line:#1c4370`, `--accent:#6db1ff`); todos los paneles (`#panel`, `#replay-panel`, `#phy-panel`, `#histpanel`) llevan un filete superior rojo de 3 px (`border-top: 3px solid var(--ucu-rojo)`). Logo: `frontend/ucuenca.png` (emblema circular "U", 269×269 RGBA, descargado del sitio oficial — `Asset-4@2x.png`); como el emblema es azul `#002856` sobre transparente, la clase `.uc-logo` le pone **fondo blanco circular con padding 2 px** (badge) para que no se pierda sobre el panel azul; va inline en el `h1` del panel principal + subtítulo `.uc-sub` "UNIVERSIDAD DE CUENCA". También existe el logotipo vectorial oficial `Nova.svg` (4 rects azul/rojo) por si se quiere la marca completa. Dos colores hardcodeados del tema viejo en `app.js` (`#2c3644`, separador del popup y grid de los canvas históricos) pasaron a `#1c4370`. Nada del backend cambia; nginx sirve `ucuenca.png` automáticamente (bind mount del dir frontend) y con `Cache-Control: no-store` no hay que purgar caché — basta recargar.

11. **Features inspiradas en gods-eye-view (29-ago, Cowork):** tras evaluar `github.com/bilawalsidhu/gods-eye-view` (MIT, pero CesiumJS+Vite — motor distinto a MapLibre+deck.gl, no es un "drop-in"), se portaron 3 patrones adaptados al stack sin build step:
   - **Cámara "cockpit"** (`app.js`): `startFollow(id)/stopFollow()/updateFollowCamera()`. Botón "🎥 Seguir este vehículo" en el popup del inspector (clic derecho) → `map.jumpTo({center:[lon,lat], bearing:v.angle, pitch:76, zoom:19.3})` en cada tick de interpolación (rumbo SUMO 0=N horario = mismo convenio que el bearing de MapLibre, sin conversión). Badge flotante `#follow-badge` con rumbo+velocidad en vivo; cualquier pan/rotate/pad manual o el botón de órbita cancelan el seguimiento y restauran la cámara previa.
   - **Looks de sensor** (`SENSOR_MODES`/`applySensorMode()`): NVG/FLIR/Noir/CRT como filtro CSS sobre `#map` (mapa+overlay deck.gl comparten el elemento) — sin shaders GLSL extra. CRT añade scanlines vía `#scanlines` (`body[data-sensor="crt"]`). Selector `#sensor-mode` en el topbar.
   - **Overlay de detección** (`makeDetectionLayer()`): caja (`PathLayer`, reutiliza `partRect()`) + etiqueta (`TextLayer`) tipo HUD sobre cada vehículo — ilustra qué "vería" un nodo V2X. Checkbox "Detección (HUD)" en el panel principal; gateado por `LOD_MAX_DETAILED` como la capa de vehículos.
   - **Heading world-stable de gods-eye-view: NO aplica** — sus íconos de avión son sprites 2D que necesitan corrección screen-space; los vehículos de SUMO-GEO ya son geometría 3D real orientada por `d.angle` (`partRect`), así que el rumbo siempre es correcto en cualquier ángulo de cámara sin truco adicional. Se usó ese presupuesto para el HUD de rumbo/velocidad del modo cockpit en su lugar.
   - Verificado: `node --check app.js` limpio, todos los IDs nuevos cruzados HTML↔JS sin huérfanos. Sin verificación visual en vivo (el navegador de Cowork no puede alcanzar un servidor localhost del sandbox) — probar con `docker compose --profile visor up -d` y recargar (nginx sirve con `Cache-Control: no-store`).

12. **Replay: barra de tiempo, velocidad y suavizado (29-ago, Cowork, 2ª tanda):**
   - Barra de tiempo re-estilada (`#replay-bar`): track con relleno de progreso (degradado inline recalculado en `updateSeekFill()` — el track nativo ignora `background` por CSS solo), thumb circular, tiempos `m:ss` (`fmtClock()`) actual/total en `#rp-time-cur/#rp-time-total`. Los `els.rpTime` viejos se eliminaron.
   - Control de velocidad del replay (`#rp-speed-row`: 🐢 `1×` 🐇): multiplicadores 0.25–4× sobre `RP_BASE_FPS=10` vía `{"cmd":"speed","fps":N}` (el backend ya lo soportaba en `_ws_replay`, sin cap). Se resetea a 1× al entrar en replay; fila visible solo en modo replay (3 puntos de toggle, como stepRow).
   - **Bugfix movimiento avanza-y-para**: `interpPeriod` es una MEDIA móvil → la mitad de los frames llegan más tarde que la estimación (jitter) y el vehículo alcanzaba su objetivo antes de tiempo, quedándose parado hasta el frame siguiente. Fix: `INTERP_STRETCH=1.30` estira la ventana de interpolación (`t = dt/(interpPeriod*STRETCH)`); el movimiento nunca se agota y, como cada frame reinicia desde la posición DIBUJADA, el pequeño retraso se recupera solo sin saltos (equivalente al "render one interval behind" de gods-eye-view).
   - Bugfix cámara cockpit: el botón "🎥 Seguir" del popup del inspector era inclickeable — heredaba el `pointer-events:none` del `#popup` no interactivo; ahora lleva `pointer-events:auto` inline.
   - **Bugfix cockpit 2 (el de verdad):** `map.jumpTo()`/`setBearing()` disparan `rotatestart` TAMBIÉN programáticamente (sin `originalEvent`) — el handler `rotatestart → stopFollow()` cancelaba el seguimiento en el primer frame de la propia cámara. Guard: solo cancelar si `e.originalEvent` existe (gesto real). El mismo evento le quitaba el `.active` al botón de órbita en cada frame (la órbita sobrevivía porque re-agenda su rAF después del setBearing).
   - **Velocidad real del replay (v2, la buena):** el backend emitía a `max_fps=10` × 0.5 s/frame → 5× tiempo real. El primer fix (2 fps × 0.5 s = 1×) era correcto pero interpolar tramos de 500 ms se veía robótico. Solución final: **cadencia de render fija a 10 fps y avance simulado variable** — el cmd `speed` del replay acepta ahora `"step"` (main.py `_ws_replay`, clamp 0.01–10 s; **REQUIERE rebuild del backend**) y el visor manda `{fps:10, step: mult/10}` (1× = 0.1 s/frame, granularidad de un CAM a 10 Hz). Se aplica automáticamente al entrar en replay; siembra `interpPeriod=100`. OJO: los botones ⏮/⏭ ahora avanzan `step` del multiplicador vigente (0.1 s a 1×), más fino que antes (0.5 s).
   - **Pulso TX = alcance real:** el anillo de transmisión ya no crece a un radio arbitrario (8+140·prog): se expande hasta `phyRangeM` = `range_m.p95` de la corrida (capturado en `loadPhyStats()`; fallback `PULSE_FALLBACK_M=150`). Desvanecimiento suavizado (alfa 220→30% al final, antes 170→0) y trazo 3.2 px para que sea visible todo el ciclo.
   - **TTL de mensajes por tipo:** `MSG_TTL` (900 ms único) se dividió en `MSG_TTL_TX=1200` (pulso) y `MSG_TTL_RX=2600` (arco, el que se inspecciona con clic) vía helper `msgTtl(e)` — los arcos duraban demasiado poco para verlos/clicarlos.
   - **Arcos adelantados a los vehículos:** los eventos TX/RX anclaban la posición cruda del frame recién llegado, pero los vehículos se pintan interpolados (~medio período atrás). `makeMessageLayers()` ahora re-ancla pulsos y arcos a la posición DIBUJADA por estación en cada tick (mapa `drawn[station]`; los eventos RX guardan `st2` = receptor; fallback a la posición capturada si el vehículo salió).

13. **Mensajes V2X y panel PHY EN VIVO (29-ago, Cowork, 3ª tanda — REQUIERE rebuild backend):** ns-3 escribe los pcap en su workdir DURANTE la corrida y ese volumen ya está montado RO en el backend (`/ns3`); ahora el modo vivo los lee en caliente:
   - `config.py`: `live_pcap_dir=/ns3/ns-3-dev` (APP_LIVE_PCAP_DIR; vacío = off), `live_refresh_s=2.0`.
   - `main.py`: `_get_live_replay()` — índice `ReplayData` sobre el workdir, recarga por huella mtime/size con throttle, tolerante a pcap truncado a medio escribir (conserva el índice previo). `/api/replay/info?live=1` sirve las stats de la corrida EN CURSO.
   - Bucle vivo del WS: deriva `station` de los dígitos del id SUMO (== stationID ETSI del mapeo del replay), consulta `window(t-6, t)` (margen porque ns-3 vuelca los pcap con retraso de buffer) y adjunta a cada frame los eventos aún no enviados (dedup por conexión `sent_v2x`, poda >20k claves).
   - Frontend: al llegar los primeros `messages` en vivo se revelan "Mensajes V2X" + filtros CAM/CPM/DENM y el panel PHY con `loadPhyStats(0, true)` + refresco cada 10 s (`liveV2xSeen`/`liveV2xTimer`, reseteados en cada meta). Pulsos/arcos se anclan por station a los vehículos dibujados, igual que en replay. El clic-para-decodificar sigue siendo solo del replay (el decode ASN.1 vive en esa rama del WS).
   - Gotchas: los eventos aparecen con unos segundos de retraso (buffering de PcapFileWrapper); en managed no hay `/ns3` → glob vacío → feature inerte sin coste.
   - **Bugfix pausas periódicas en vivo (regresión de esta feature):** el bucle de frames hacía `await asyncio.to_thread(re-parse)` y, como el pcap CRECE, la huella cambiaba en cada chequeo → el stream se congelaba ~0.5 s (y creciendo) cada ~2 s. Ahora `_live_rep_nowait()` devuelve el caché al instante y el re-parse corre como tarea de fondo (`_live_refresh_bg`) que intercambia el índice al terminar; el throttle se adapta a `3×` la duración real del parse (corridas largas no saturan CPU). `phy_stats()` y `_get_replay()` del endpoint REST también pasaron a `to_thread` (la 1ª llamada por índice es costosa y bloqueaba el event loop).
   - **UI panel SUMO·GEO (29-ago):** eliminado el selector "Escenario" (Bajo/Medio/Alto — solo aplicaba en managed; en modo van3twin no debía usarse). El WS sigue enviando `?level=mid` fijo (`currentLevel` quedó como constante, inocuo). "Detección (HUD)" movido encima de "Calles concurridas".
   - **Bugfix micro-pausas en vivo, 2ª ronda (contención de GIL):** aun con el parse en thread, el trabajo es Python puro y retiene el GIL → ráfagas que frenaban el event loop. Tres fuentes eliminadas: (1) `_SPEC_CACHE` a nivel módulo en replay.py — la spec CAM (~0.3 s de compile) se compilaba POR INSTANCIA, o sea en cada recarga del índice vivo; (2) `ReplayData(live=True)` — `_station_segments` decodifica solo el ÚLTIMO CAM por nodo (mapeo vigente, 12 decodes) en vez de TODOS (miles, creciendo con la corrida); (3) `phy_stats()` en vivo ya no se calcula en la petición del panel (era O(eventos) cada 10 s sobre un índice recién estrenado sin caché): se calcula en `_live_build` (thread de fondo) con throttle `_LIVE_PHY_EVERY_S=12` y el endpoint `?live=1` sirve el caché `_live["phy"]`; el poll del frontend pasó a 15 s. Trade-off documentado del modo live: eventos antiguos a caballo de una reutilización del pool de nodos pueden atribuirse al station vigente (irrelevante para la ventana de ~6 s que se muestra). Smoke test del parser live con pcap sintético: OK.
   - **Filtros en vivo:** el selector de emisor salió de `#replay-step-row` a su propia fila `#row-veh-filter` ("Emisor"), visible en replay (meta) y en vivo (al detectar V2X, junto con los filtros CAM/CPM/DENM que ya se revelaban). En vivo las opciones se pueblan dinámicamente con las estaciones que van apareciendo (`liveStations`, conservando la selección si sigue existiendo); se resetean en cada meta. El anillo ámbar del emisor seleccionado funciona en vivo porque los vehículos ya traen `station`.

14. **Vehículos 3D glTF instanciados (29-ago, Cowork, 4ª tanda — solo frontend):** los vehículos dejan de ser cajas: `ScenegraphLayer` con un `.glb` low-poly por clase en `frontend/models/` (car 308 tris tintable por paleta / bus 424 naranja / ambulance 312 con franja roja y barra de luces azul; ~10 KB c/u; generados proceduralmente con trimesh — script reproducible en la sesión Cowork; **CC-propio, sin licencias de terceros**; construidos Z-up, morro a +Y, tamaño real, suelo z=0). Instancing GPU → MÁS rápido que las cajas (que regeneraban 8 polígonos/veh/tick en CPU). Claves:
   - `deck.gl@9 dist.min.js` trae ScenegraphLayer pero **NO el GLTFLoader**: index.html carga además `@loaders.gl/gltf@4.2.2/dist/dist.min.js` (global `loaders`) y la capa pasa `loaders: [loaders.GLTFLoader]` (verificado contra los bundles reales de unpkg).
   - `makeVehiclesLayer()` devuelve ahora ARRAY: modelos si `vehModelsOk && ≤ MODEL_MAX_VEH=3000`, si no `makeVehicleBoxLayer()` (las cajas de siempre, intactas como fallback). `vehModelsOk` se apaga solo con `onError` de la capa (p. ej. si interleaved+glTF fallara, como pasó con IconLayer) o si faltan los globals.
   - Orientación: SUMO 0=N horario vs yaw deck antihorario → `MODEL_YAW = a => -a`. **Si el modelo aparece girado al probar, ajustar SOLO esa función.** Escala por vehículo con `getScale` (len/wid reales de SUMO / dims base del glb). Tinte multiplicativo `getColor`: solo el sedán (carrocería gris claro en el glb) toma la paleta; clase por regex `vehClass()` (emergency/bus/car).
   - Picking: helper `pickedVehicle(info)` unifica cajas (pieza `.veh`) y glb (el objeto ES el vehículo) para inspector y botón Seguir. `DYN_IDS` incluye `veh-glb-*`.
   - Para cambiar a modelos de terceros (p. ej. Kenney Car Kit CC0): reemplazar los `.glb` en `frontend/models/` y actualizar `len/wid` base en `VEH_MODELS` (+ quizá `MODEL_YAW` según los ejes del asset).
   - **Modelos Kenney Car Kit (CC0) activos (29-ago, noche):** descargado `kenney_car-kit.zip` de kenney.nl y convertidos 6 .glb a la convención propia con trimesh (transform `x→−x, y↔z` — det +1, sin espejo — + suelo a z=0; texturas verificadas intactas): 5 variantes de auto (sedan/suv/taxi/van/hatchback-sports, elegida por `hashId(id) % n` — estable entre frames, una ScenegraphLayer por variante, id `veh-glb-<clase>-<idx>`) + ambulance. El kit NO trae bus → se conserva el `bus.glb` procedural naranja. `VEH_MODELS` ahora es lista de variantes por clase con `len/wid` base medidos y `yawOff` opcional (si los Kenney aparecieran marcha atrás → `yawOff: 180`; el morro se asumió +Z del glTF original). El tinte por paleta ya NO aplica a los modelos (color en textura); `CAR_COLORS` queda para el fallback de cajas. `isDynLayer()` reemplaza al set fijo para los ids dinámicos. Los .glb procedurales (car/ambulance propios) siguen en `models/` por si se quiere volver.
   - Paleta nueva "atardecer andino" en `CAR_COLORS` (10 tonos medios: crema, terracota, mostaza, salvia, turquesa, cobalto, ciruela, coral, pizarra, cobre) — elegidos para el tinte multiplicativo del glb (tonos muy oscuros dejarían manchas negras); aplica igual al fallback de cajas.

**Puntos de atención detectados en el código**:
- Cada conexión WS crea un `SumoBridge` nuevo y `close()` al desconectar → en remote, recargar la página cierra la conexión TraCI del cliente 2 (con `--num-clients 2` el run puede quedar inutilizable; "un visor por corrida", según la guía).
- El selector Bajo/Medio/Alto solo aplica en managed (busca `metro_{level}.sumocfg`); en modo van3twin no debe usarse.
- La rama remote ignora `config`/`begin`; usa el módulo `traci` global (sin labels) → una sola conexión remote por proceso.
- `lifespan` necesita `net_file` **y** `poly_file` para no caer al parseo del `.sumocfg` (de ahí `APP_POLY_FILE=/no_poly.xml`). Mejora opcional: tolerar `APP_POLY_FILE=""`.
- `requirements.txt` pina 1.27.1; el contenedor van3twin lleva SUMO **1.12.0** → los pines deben coincidir con el SUMO al que se conecta.
