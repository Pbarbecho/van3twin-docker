# Espejo del árbol ns-3 (dañado por APFS case-insensitive; el código vive en
# el volumen Docker y su respaldo es el fork Pbarbecho/VaN3Twin)
VaN3Twin/

# Salidas de simulación pesadas (respaldar aparte si interesan)
results/*.pcap
backups/

# Auxiliares de LaTeX y logs
*.aux
*.log
*.out
*.toc
build.log
.DS_Store

# configuración local por máquina (rutas propias)
.env

# documentación de trabajo y utilidades locales (fuera del repo público)
Intergracion SUMO 3D WEB VAN3TWIN/
RESPALDO.md
sync-mac-edits.sh
